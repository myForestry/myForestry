import React from 'react';

const Header = (props) => {
  return (
    <div>
      Hello from Header
    </div>
  );
};

Header.displayName = 'Header';

Header.propTypes = {};

export default Header;
