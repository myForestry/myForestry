import React from 'react';

const Profile = (props) => {
  return (
    <div>
      Hello from Profile
    </div>
  );
};

Profile.displayName = 'Profile';

Profile.propTypes = {};

export default Profile;
