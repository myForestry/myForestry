import React from 'react';

const Search = (props) => {
  return (
    <div>
      Hello from Search
    </div>
  );
};

Search.displayName = 'Search';

Search.propTypes = {};

export default Search;
